import 'package:flutter/material.dart';

class PhotGallery extends StatelessWidget {
  const PhotGallery({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
